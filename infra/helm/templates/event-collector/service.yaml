{{- if .Values.eventCollector.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "recsys.fullname" . }}-event-collector
  labels:
    {{- include "recsys.serviceLabels" (dict "Release" .Release "Chart" .Chart "serviceName" "event-collector") | nindent 4 }}
spec:
  type: {{ .Values.eventCollector.service.type }}
  ports:
    - name: http
      port: {{ .Values.eventCollector.service.port }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.eventCollector.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "recsys.selectorLabels" (dict "Release" .Release "serviceName" "event-collector") | nindent 4 }}
{{- end }}
